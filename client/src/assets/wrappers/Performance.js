import styled from "styled-components";

const Wrapper = styled.section`
  input[type="radio"] {
    border: 0px;
    width: 2rem;
    height: 3rem;
    margin: 1rem;
  }
  .performance-card {
  }
  label {
    display: block;
    font-size: 1.75rem;
  }
`;

export default Wrapper;
